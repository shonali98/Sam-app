{
    "configurations": [
        {
            "type": "aws-sam",
            "request": "direct-invoke",
            "name": "build:HelloWorldFunction",
            "invokeTarget": {
                "target": "template",
                "templatePath": "${workspaceFolder}/sam-app/.aws-sam/build/template.yaml",
                "logicalId": "HelloWorldFunction"
            },
            "lambda": {
                "payload": {},
                "environmentVariables": {}
            }
        }
    ]
}